<section id="solution" class="py-20 md:py-28 bg-gray-200 scroll-mt-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section heading -->
    <div class="max-w-3xl mx-auto text-center mb-28">
      <h2 class="text-4xl md:text-5xl font-extrabold text-navy mb-6 animate-on-scroll">
        Faster Techs.<br>
        Better Documentation.<br>
        More Revenue Per RO.
      </h2>
      <p class="text-xl text-gray-600 leading-relaxed animate-on-scroll stagger-2">
        RO.bot gives your technicians voice-first AI tools across the entire service workflow.
        They talk instead of type. AI handles the rest.
      </p>
    </div>

    <!-- Pillar 1: Get Paid for More Work -->
    <div id="3cs" class="mb-36 scroll-mt-28">
      <div class="grid md:grid-cols-2 gap-12 items-start">
        <div class="animate-on-scroll">
          <span class="text-teal font-bold text-sm uppercase tracking-wider">Get Paid for More Work</span>
          <h3 class="text-3xl md:text-4xl font-bold text-navy mt-3 mb-5">
            AI-Powered Repair Stories
          </h3>
          <p class="text-xl text-gray-600 leading-relaxed mb-6">
            Techs talk, RO.bot does the writing. Voice notes become professional, warranty-compliant repair stories in seconds. Every story gets graded on warranty-specific criteria so techs know exactly how to improve.
          </p>

          <!-- Feature bullets -->
          <ul class="space-y-3 mb-6">
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">Save 5 - 35 minutes per RO</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">AI grader catches issues and guides techs to fix</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">Reduced burden for warranty admins, service managers and writers</span>
            </li>
          </ul>

          <!-- Stat callout -->
          <div class="bg-teal/10 rounded-xl p-5 mb-5">
            <div class="flex items-baseline gap-2">
              <span class="text-4xl font-extrabold text-teal">+15%</span>
              <span class="text-teal text-[28px]">warranty profit</span>
            </div>
            <p class="text-sm text-gray-400 mt-1">AutoTalk</p>
          </div>

          <!-- CTA link -->
          <a href="/book-demo" class="inline-flex items-center gap-2 text-teal font-semibold hover:text-teal-dark transition-colors">
            See AI-enhanced 3Cs in action
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
        <div class="flex justify-center animate-scale-in stagger-2" id="robo-demo">
          <!-- Fixed-size container for stacked cards with 20px offsets -->
          <div class="relative w-full" style="height: 520px;">
            <!-- Card 1: Recording (top-left, fixed size) -->
            <div id="card-recording" class="demo-card absolute bg-white rounded-2xl p-5 md:p-6 border border-teal/20 shadow-lg" style="top: 0; left: 0; right: 40px; height: 300px; z-index: 1; overflow: hidden;">
              <div class="flex items-center justify-between mb-4">
                <h4 class="font-bold text-navy text-base">Voice Capture</h4>
              </div>
              <div id="phase-voice">
                <div class="flex items-center gap-2 mb-2">
                  <div class="w-3 h-3 rounded-full bg-red-500 animate-pulse" id="rec-dot"></div>
                  <span class="text-sm text-gray-500" id="rec-label">Recording...</span>
                </div>
                <div class="flex items-center gap-[3px] h-6 mb-2" id="waveform">
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                  <div class="waveform-bar w-1 bg-teal/60 rounded-full"></div>
                </div>
                <div class="bg-gray-50 rounded-lg border border-gray-200 p-3 text-sm text-gray-600 italic h-[160px] overflow-hidden">
                  <span id="voice-text"></span>
                </div>
              </div>
            </div>

            <!-- Card 2: Writing / 3Cs Story (offset 20px down & right) -->
            <div id="card-writing" class="demo-card absolute bg-white rounded-2xl p-5 md:p-6 border border-teal/20 shadow-lg opacity-0" style="top: 20px; left: 20px; right: 20px; height: 420px; z-index: 2; overflow: hidden; transform: translateY(-40px);">
              <div class="flex items-center justify-between mb-4">
                <h4 class="font-bold text-navy text-base">3Cs Story</h4>
              </div>
              <!-- Content area (relative, for spinner overlay) -->
              <div class="relative">
                <!-- 3Cs content (always rendered, determines card size) -->
                <div id="story-content">
                  <div class="space-y-2">
                    <div class="bg-gray-50 rounded-lg p-2.5 border-l-4 border-blue-500">
                      <div class="text-xs font-bold text-blue-600 uppercase tracking-wider mb-0.5">Complaint</div>
                      <p class="text-sm text-gray-700 leading-snug">Customer states the vehicle is overheating.</p>
                    </div>
                    <div class="bg-gray-50 rounded-lg p-2.5 border-l-4 border-blue-500">
                      <div class="text-xs font-bold text-blue-600 uppercase tracking-wider mb-0.5">Cause</div>
                      <p class="text-sm text-gray-700 leading-snug">Customer concern regarding vehicle overheating verified. The engine was observed reaching elevated temperatures after approximately ten minutes at idle. Inspection of the cooling system revealed coolant leaking from the water pump weep hole, indicating internal seal failure.</p>
                    </div>
                    <div class="bg-gray-50 rounded-lg p-2.5 border-l-4 border-blue-500">
                      <div class="text-xs font-bold text-blue-600 uppercase tracking-wider mb-0.5">Correction</div>
                      <p class="text-sm text-gray-700 leading-snug">Replaced the water pump due to weep hole seal failure. Replaced the thermostat as a preventive measure. Refilled the cooling system with manufacturer-specified coolant and performed a full system bleed. Confirmed no leaks and temperature within normal range.</p>
                    </div>
                  </div>
                </div>
                <!-- Writing spinner overlay (covers content until done) -->
                <div id="writing-spinner" class="absolute inset-0 bg-white flex flex-col items-center justify-center gap-3">
                  <div class="w-8 h-8 border-[3px] border-teal border-t-transparent rounded-full animate-spin"></div>
                  <span class="text-base text-gray-500">Writing 3Cs story...</span>
                </div>
              </div>
            </div>

            <!-- Card 3: Grading / Grade (offset 40px down & right, same height as card 2) -->
            <div id="card-grading" class="demo-card absolute bg-white rounded-2xl p-5 md:p-6 border border-teal/20 shadow-lg opacity-0" style="top: 40px; left: 40px; right: 0; height: 420px; z-index: 3; overflow: hidden; transform: translateY(-40px);">
              <div class="flex items-center justify-between mb-4">
                <h4 class="font-bold text-navy text-base">Story Grade</h4>
              </div>
              <!-- Content area (relative, for spinner overlay) -->
              <div class="relative">
                <!-- Grade content (always rendered) -->
                <div id="grade-content">
                  <div class="bg-gray-50 rounded-lg p-3 border border-teal/20 mb-3">
                    <div class="flex items-center gap-2 mb-2">
                      <span class="text-sm text-gray-500">Overall Story Grade</span>
                      <span class="text-2xl font-extrabold text-teal">A-</span>
                    </div>
                    <div class="grid grid-cols-2 gap-1.5 text-sm">
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-green-100 text-green-700 font-bold flex items-center justify-center text-xs">A</span> Customer Complaint</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-green-100 text-green-700 font-bold flex items-center justify-center text-xs">A</span> Complaint Verification</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-yellow-100 text-yellow-700 font-bold flex items-center justify-center text-xs">B</span> Diagnostics Performed</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-green-100 text-green-700 font-bold flex items-center justify-center text-xs">A</span> Component Involved</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-green-100 text-green-700 font-bold flex items-center justify-center text-xs">A</span> Root Cause</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-green-100 text-green-700 font-bold flex items-center justify-center text-xs">A</span> Repair Action</div>
                      <div class="flex items-center gap-1.5"><span class="w-5 h-5 rounded bg-yellow-100 text-yellow-700 font-bold flex items-center justify-center text-xs">B</span> Final Verification</div>
                    </div>
                  </div>
                  <!-- Suggestions to improve -->
                  <div class="border-t border-gray-200 pt-3">
                    <div class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-2">Suggestions to reach A+</div>
                    <div class="space-y-1.5">
                      <div class="flex items-start gap-2 text-sm text-gray-600">
                        <span class="text-yellow-500 mt-0.5 flex-shrink-0">&#9679;</span>
                        <span><span class="font-semibold text-gray-700">Diagnostics:</span> Include specific pressure test readings and temperature measurements.</span>
                      </div>
                      <div class="flex items-start gap-2 text-sm text-gray-600">
                        <span class="text-yellow-500 mt-0.5 flex-shrink-0">&#9679;</span>
                        <span><span class="font-semibold text-gray-700">Final Verification:</span> Document a post-repair test drive with duration and conditions.</span>
                      </div>
                    </div>
                  </div>
                </div>
                <!-- Grading spinner overlay (covers content until done) -->
                <div id="grading-spinner" class="absolute inset-0 bg-white flex flex-col items-center justify-center gap-3">
                  <div class="w-8 h-8 border-[3px] border-teal border-t-transparent rounded-full animate-spin"></div>
                  <span class="text-base text-gray-500">Grading story...</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- Pillar 2: Sell More Work -->
    <div id="video" class="mb-36 scroll-mt-28">
      <div class="grid md:grid-cols-2 gap-12 items-start">
        <div class="order-2 md:order-1 flex justify-center animate-scale-in">
          <img
            src="/teleprompter-screens/calipers.PNG"
            alt="AI-guided video inspection teleprompter"
            class="w-64 md:w-72 rounded-3xl shadow-2xl"
            loading="lazy"
          >
        </div>
        <div class="order-1 md:order-2 animate-on-scroll stagger-2">
          <span class="text-teal font-bold text-sm uppercase tracking-wider">Sell More Work</span>
          <h3 class="text-3xl md:text-4xl font-bold text-navy mt-3 mb-5">
            AI-Guided Video Inspections
          </h3>
          <p class="text-xl text-gray-600 leading-relaxed mb-6">
            RO.bot writes the script and provides an on-screen teleprompter, so every video is clear, professional, and customer-ready.
          </p>

          <!-- Feature bullets -->
          <ul class="space-y-3 mb-6">
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">AI writes a script based on MPI and diagnostics</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">Professional videos that drive higher acceptance rates, more revenue per RO</span>
            </li>
          </ul>

          <!-- Stat callout -->
          <div class="bg-teal/10 rounded-xl p-5 mb-5">
            <div class="flex items-baseline gap-2">
              <span class="text-4xl font-extrabold text-teal">3x</span>
              <span class="text-teal text-[28px]">more likely to approve repairs</span>
            </div>
            <p class="text-sm text-gray-400 mt-1">JD Power</p>
          </div>

          <!-- CTA link -->
          <a href="/book-demo" class="inline-flex items-center gap-2 text-teal font-semibold hover:text-teal-dark transition-colors">
            See video inspections in action
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- Pillar 3: Find More Work -->
    <div id="mpi" class="scroll-mt-28">
      <div class="grid md:grid-cols-2 gap-12 items-start">
        <div class="animate-on-scroll">
          <span class="text-teal font-bold text-sm uppercase tracking-wider">Find More Work</span>
          <h3 class="text-3xl md:text-4xl font-bold text-navy mt-3 mb-5">
            Voice-First MPI
          </h3>
          <p class="text-xl text-gray-600 leading-relaxed mb-6">
            Techs call out findings as they go. No clipboard, no buttons. Higher completion rates = more issues discovered on every vehicle.
          </p>

          <!-- Feature bullets -->
          <ul class="space-y-3 mb-6">
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">No clicking</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">90% faster to score</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">Custom technician notes automatically attached</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-teal mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="text-teal text-lg">30% higher completion rates</span>
            </li>
          </ul>

          <!-- Stat callout -->
          <div class="bg-teal/10 rounded-xl p-5 mb-5">
            <div class="flex items-baseline gap-2">
              <span class="text-4xl font-extrabold text-teal">+59%</span>
              <span class="text-teal text-[28px]">customer-paid profit</span>
            </div>
            <p class="text-sm text-gray-400 mt-1">CBT News</p>
          </div>

          <!-- CTA link -->
          <a href="/book-demo" class="inline-flex items-center gap-2 text-teal font-semibold hover:text-teal-dark transition-colors">
            See MPI in action
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
        <!-- MPI Animation -->
        <div class="flex justify-center animate-scale-in stagger-2">
          <div class="relative" id="mpi-animation">
            <img
              src="/MPI-screens/tires_voicenotes.PNG"
              alt="MPI with voice notes"
              class="w-64 md:w-72 rounded-3xl shadow-2xl transition-opacity duration-700"
              id="mpi-before"
            >
            <img
              src="/MPI-screens/tires_findings_applied.PNG"
              alt="MPI with findings applied"
              class="w-64 md:w-72 rounded-3xl shadow-2xl absolute inset-0 opacity-0 transition-opacity duration-700"
              id="mpi-after"
            >
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<script>
  // MPI animation: scroll-triggered toggle between voice notes and findings applied
  const mpiBefore = document.getElementById('mpi-before');
  const mpiAfter = document.getElementById('mpi-after');
  const mpiContainer = document.getElementById('mpi-animation');

  let mpiRunId = 0;

  function resetMpi() {
    if (mpiBefore) mpiBefore.style.opacity = '1';
    if (mpiAfter) mpiAfter.style.opacity = '0';
  }

  async function runMpiLoop(id) {
    while (id === mpiRunId) {
      // Show "before" for 3s
      await sleep(3000);
      if (id !== mpiRunId) return;
      // Swap to "after"
      if (mpiBefore) mpiBefore.style.opacity = '0';
      if (mpiAfter) mpiAfter.style.opacity = '1';
      // Hold "after" for 3s
      await sleep(3000);
      if (id !== mpiRunId) return;
      // Swap back to "before"
      if (mpiBefore) mpiBefore.style.opacity = '1';
      if (mpiAfter) mpiAfter.style.opacity = '0';
    }
  }

  if (mpiContainer) {
    const mpiObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        mpiRunId++;
        resetMpi();
        if (entry.isIntersecting) {
          const id = mpiRunId;
          sleep(800).then(() => {
            if (id === mpiRunId) runMpiLoop(id);
          });
        }
      });
    }, { threshold: 0.3 });
    mpiObserver.observe(mpiContainer);
  }

  // RO.bot stacked card demo animation
  const voiceTextEl = document.getElementById('voice-text');
  const waveformEl = document.getElementById('waveform');
  const recDot = document.getElementById('rec-dot');
  const recLabel = document.getElementById('rec-label');

  const cardRecording = document.getElementById('card-recording');
  const cardWriting = document.getElementById('card-writing');
  const cardGrading = document.getElementById('card-grading');

  const writingSpinner = document.getElementById('writing-spinner');
  const storyContent = document.getElementById('story-content');
  const gradingSpinner = document.getElementById('grading-spinner');
  const gradeContent = document.getElementById('grade-content');

  // Voice transcript with per-word delay (ms AFTER this word appears).
  // Bursts of fast words (~80-120ms), natural pauses at commas/periods (~400-600ms),
  // longer hesitations at "Uh," and "..." (~700-900ms).
  const voiceScript = [
    ["Ok,", 350], ["I", 80], ["verified", 100], ["the", 70], ["overheating", 120], ["issue,", 450],
    ["let", 90], ["it", 70], ["idle", 110], ["for", 80], ["about", 100], ["ten", 90], ["minutes", 130],
    ["and", 100], ["yeah", 120], ["the", 70], ["gauge", 110], ["climbed", 100], ["right", 80], ["up.", 600],
    ["The", 100], ["water", 90], ["pump's", 110], ["leaking", 100], ["from", 80], ["the", 70], ["weep", 110], ["hole.", 700],
    ["Uh,", 800], ["the", 90], ["thermostat", 120], ["was", 80], ["also...", 900],
    ["actually", 110], ["it", 70], ["was", 80], ["working", 100], ["but", 90], ["not", 80], ["perfect", 120],
    ["so", 90], ["I'm", 80], ["replacing", 110], ["that.", 550],
    ["I", 80], ["put", 70], ["in", 60], ["the", 70], ["new", 80], ["pump", 90], ["and", 70], ["thermostat,", 400],
    ["refilled", 110], ["the", 70], ["coolant,", 450],
    ["let", 80], ["it", 70], ["run", 80], ["for", 80], ["uh...", 850], ["fifteen", 120], ["minutes.", 600],
    ["No", 90], ["leaks,", 350], ["temp", 100], ["is", 70], ["good.", 300],
  ];

  let runId = 0; // incremented on each start; stale loops check this to self-cancel

  const demoObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        // Cancel any running animation, reset, then start fresh
        runId++;
        resetDemo();
        startAfterDelay(runId);
      } else {
        // Scrolled away: cancel running animation
        runId++;
        resetDemo();
      }
    });
  }, { threshold: 0.3 });

  const demoContainer = document.getElementById('robo-demo');
  if (demoContainer) demoObserver.observe(demoContainer);

  async function startAfterDelay(id) {
    await sleep(800);
    if (id !== runId) return; // cancelled
    runDemoLoop(id);
  }

  async function runDemoLoop(id) {
    while (id === runId) {
      await runDemo(id);
      if (id !== runId) return;
      await sleep(5000);
      if (id !== runId) return;
      resetDemo();
    }
  }

  function resetDemo() {
    // Disable transitions so reset is instant
    const cards = [cardRecording, cardWriting, cardGrading];
    cards.forEach(c => { if (c) c.style.transition = 'none'; });
    if (writingSpinner) writingSpinner.style.transition = 'none';
    if (gradingSpinner) gradingSpinner.style.transition = 'none';

    // Reset recording card
    if (voiceTextEl) voiceTextEl.textContent = '';
    if (waveformEl) waveformEl.classList.remove('waveform-active');
    if (recDot) recDot.classList.add('animate-pulse');
    if (recLabel) recLabel.textContent = 'Recording...';

    // Show only recording card, hide others (reset to above-card position)
    if (cardRecording) {
      cardRecording.style.opacity = '1';
    }
    if (cardWriting) {
      cardWriting.style.opacity = '0';
      cardWriting.style.transform = 'translateY(-40px)';
    }
    if (cardGrading) {
      cardGrading.style.opacity = '0';
      cardGrading.style.transform = 'translateY(-40px)';
    }

    // Reset spinner overlays (visible, covering content)
    if (writingSpinner) { writingSpinner.style.opacity = '1'; writingSpinner.style.pointerEvents = 'auto'; }
    if (gradingSpinner) { gradingSpinner.style.opacity = '1'; gradingSpinner.style.pointerEvents = 'auto'; }

    // Re-enable transitions on next frame
    requestAnimationFrame(() => {
      cards.forEach(c => { if (c) c.style.transition = ''; });
      if (writingSpinner) writingSpinner.style.transition = '';
      if (gradingSpinner) gradingSpinner.style.transition = '';
    });
  }

  async function runDemo(id) {
    // Start voice recording animation
    if (waveformEl) waveformEl.classList.add('waveform-active');

    // Run voice transcript in background (don't await)
    runVoiceTranscript(id);

    // After 4 seconds, show card 2 (writing spinner)
    await sleep(4000);
    if (id !== runId) return;
    if (cardWriting) {
      cardWriting.style.opacity = '1';
      cardWriting.style.transform = 'translateY(0)';
    }

    // After 2.5 seconds, reveal 3Cs story
    await sleep(2500);
    if (id !== runId) return;
    if (writingSpinner) { writingSpinner.style.opacity = '0'; writingSpinner.style.pointerEvents = 'none'; }

    // After 2 seconds, show card 3 (grading spinner)
    await sleep(2000);
    if (id !== runId) return;
    if (cardGrading) {
      cardGrading.style.opacity = '1';
      cardGrading.style.transform = 'translateY(0)';
    }

    // After 2.5 seconds, reveal grade
    await sleep(2500);
    if (id !== runId) return;
    if (gradingSpinner) { gradingSpinner.style.opacity = '0'; gradingSpinner.style.pointerEvents = 'none'; }
  }

  async function runVoiceTranscript(id) {
    let transcript = '';
    for (let i = 0; i < voiceScript.length; i++) {
      if (id !== runId) return;
      const [word, delay] = voiceScript[i];
      transcript += (i > 0 ? ' ' : '') + word;
      if (voiceTextEl) voiceTextEl.textContent = transcript;
      await sleep(delay);
    }
    if (id !== runId) return;
    if (waveformEl) waveformEl.classList.remove('waveform-active');
    if (recDot) recDot.classList.remove('animate-pulse');
    if (recLabel) recLabel.textContent = 'Done';
  }
  function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }
</script>

<style>
  .demo-card {
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  #writing-spinner, #grading-spinner {
    transition: opacity 0.5s ease;
  }
  @keyframes waveform-pulse {
    0%, 100% { height: 4px; }
    50% { height: 22px; }
  }
  .waveform-bar {
    height: 4px;
    transition: height 0.15s ease;
  }
  .waveform-active .waveform-bar {
    animation: waveform-pulse 0.6s ease-in-out infinite;
  }
  .waveform-active .waveform-bar:nth-child(1) { animation-delay: 0.00s; }
  .waveform-active .waveform-bar:nth-child(2) { animation-delay: 0.05s; }
  .waveform-active .waveform-bar:nth-child(3) { animation-delay: 0.10s; }
  .waveform-active .waveform-bar:nth-child(4) { animation-delay: 0.15s; }
  .waveform-active .waveform-bar:nth-child(5) { animation-delay: 0.20s; }
  .waveform-active .waveform-bar:nth-child(6) { animation-delay: 0.25s; }
  .waveform-active .waveform-bar:nth-child(7) { animation-delay: 0.30s; }
  .waveform-active .waveform-bar:nth-child(8) { animation-delay: 0.35s; }
  .waveform-active .waveform-bar:nth-child(9) { animation-delay: 0.40s; }
  .waveform-active .waveform-bar:nth-child(10) { animation-delay: 0.45s; }
  .waveform-active .waveform-bar:nth-child(11) { animation-delay: 0.50s; }
  .waveform-active .waveform-bar:nth-child(12) { animation-delay: 0.55s; }
  .waveform-active .waveform-bar:nth-child(13) { animation-delay: 0.60s; }
  .waveform-active .waveform-bar:nth-child(14) { animation-delay: 0.65s; }
  .waveform-active .waveform-bar:nth-child(15) { animation-delay: 0.70s; }
  .waveform-active .waveform-bar:nth-child(16) { animation-delay: 0.75s; }
  .waveform-active .waveform-bar:nth-child(17) { animation-delay: 0.80s; }
  .waveform-active .waveform-bar:nth-child(18) { animation-delay: 0.85s; }
  .waveform-active .waveform-bar:nth-child(19) { animation-delay: 0.90s; }
  .waveform-active .waveform-bar:nth-child(20) { animation-delay: 0.95s; }
  .waveform-active .waveform-bar:nth-child(21) { animation-delay: 1.00s; }
  .waveform-active .waveform-bar:nth-child(22) { animation-delay: 1.05s; }
  .waveform-active .waveform-bar:nth-child(23) { animation-delay: 1.10s; }
  .waveform-active .waveform-bar:nth-child(24) { animation-delay: 1.15s; }
</style>
